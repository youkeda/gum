import { Vue } from 'vue-property-decorator';
export default class RightDrawer extends Vue {
    private visible?;
    private title?;
    private showImage?;
    private width?;
    private className?;
    onClose(): void;
}
